import React from "react";

const Button = () => {
  return (
    <input
      className="bg-slate-700 text-white p-2 rounded cursor-pointer hover:bg-slate-800 font-medium"
      type="submit"
      value="Search"
    />
  );
};

export default Button;
